# # ── Zenity file/playlist pickers ────────────────────────────────
CTRL+p run bash -c 'p=$(zenity --file-selection --file-filter="Playlists | *.m3u *.m3u8"); [ -n "$p" ] && printf "loadlist %s\n" "$p" | socat - /run/user/1000/mpvsocket' &
